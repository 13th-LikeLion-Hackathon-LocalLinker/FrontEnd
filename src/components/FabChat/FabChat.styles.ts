import styled from '@emotion/styled';

export const ChatImg = styled.img`
  position: fixed;
  left: calc(50% + 110px);
  bottom: 98px;
  width: 60px;
  height: 60px;
  cursor: pointer;
  user-select: none;
`;

export const TopImg = styled.img`
  position: fixed;
  left: calc(50% + 110px);
  bottom: 38px;
  width: 60px;
  height: 60px;
  cursor: pointer;
  user-select: none;
`;
